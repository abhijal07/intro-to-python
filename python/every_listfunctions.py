#append()-adds an element at the last of the list
#clear()removes all the elements from the list
#copy()-returns a copy of the list
#count()-returns the number if elements with the speecified values
#extend()-add the elements of the list or iterables to the end of the list
#index()-returns the index of the first element with the specified value
#insert()-adds an element at the specified spot
#pop()-Removes the element at the specified spot
#remove()-removes the element with the specified value
#sort()-sorts the list
#reverse()-reverse the order of the list
#deln[x:y]to delete the elements between the interval x and y-1
n=list(eval(input("enter the list elements:")))
#m=list(eval(input("enter the 2nd list elements:")))
n.sort()
print(n)